(loadFrom "kiss-express" "src/ServerUtil.kiss")

(defMacro scriptContent []
    (File.getContent "frontend.js"))

(kissExpress app [3000]
    (app.get "/"
        ->[req res next]
            (res.send "<head><title>${title}</title><script>$(scriptContent)</script></head> Hello world from the server!")))
